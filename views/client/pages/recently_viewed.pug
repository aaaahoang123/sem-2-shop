- if (rvProducts && rvProducts.length !== 0)
    style.
        .char-25 {
            white-space: nowrap;
            text-overflow: ellipsis;
            overflow: hidden;
            max-width: 25ch;
        }

    .viewed
        .container
            .row
                .col
                    .viewed_title_container
                        h3.viewed_title Recently Viewed
                        .viewed_nav_container
                            .viewed_nav.viewed_prev
                                i.fas.fa-chevron-left
                            .viewed_nav.viewed_next
                                i.fas.fa-chevron-right
                    - if (rvProducts.length>6)
                        .viewed_slider_container
                            // Recently Viewed Slider
                            .owl-carousel.owl-theme.viewed_slider
                                // Recently Viewed Item
                                - for (let i=0; i<rvProducts.length; i++)
                                    .owl-item
                                        .viewed_item.discount.d-flex.flex-column.align-items-center.justify-content-center.text-center
                                            a(href='/product/'+rvProducts[i].code)
                                                .viewed_image
                                                    img(src=rvProducts[i].images[0] alt='')
                                                .viewed_content.text-center
                                                    .viewed_price $ #{rvProducts[i].price}
                                                    .viewed_name
                                                        a.product_code(href='/product/'+rvProducts[i].code title=rvProducts[i].name)
                                                            p.char-25 #{rvProducts[i].name}
                    - if (rvProducts.length<=6)
                        .viewed_slider_container
                            .row
                                - for (let i=0; i<rvProducts.length; i++)
                                    .col-sm-2
                                        .row
                                            .owl-item.col-sm-11
                                                .viewed_item.discount.d-flex.flex-column.align-items-center.justify-content-center.text-center
                                            
                                                        .viewed_image
                                                            a(href='/product/'+rvProducts[i].code): img(src=rvProducts[i].images[0] alt='')
                                                        .viewed_content.text-center
                                                            .viewed_price $ #{rvProducts[i].price}
                                                            .viewed_name
                                                                a.product_code(href='/product/' + rvProducts[i].code title=rvProducts[i].name)
                                                                    p.char-25 #{rvProducts[i].name}
// Brands
-if (brands)
    .brands
       .container
           .row
               .col
                   .brands_slider_container
                       // Brands Slider
                       .owl-carousel.owl-theme.brands_slider
                           - for (let b of brands)
                               .owl-item
                                   .brands_item.d-flex.flex-column.justify-content-center
                                       div(style='max-width: 100px; overflow: hidden')
                                           a(href='/shop?brand='+b.name)
                                                img(src=b.logo alt='')
                       // Brands Slider Navigation
                       .brands_nav.brands_prev
                           i.fas.fa-chevron-left
                       .brands_nav.brands_next
                           i.fas.fa-chevron-right